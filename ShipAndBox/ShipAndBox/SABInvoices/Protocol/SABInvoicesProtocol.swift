//
//  SABInvoicesProtocol.swift
//  ShipAndBox
//
//  Created by IsitaFS003 on 02/08/21.
//  
//

import Foundation
import UIKit
/// Protocolo que define los métodos y atributos para el view de SABInvoices
protocol SABInvoicesViewProtocol {
    // PRESENTER -> VIEW
}
/// Protocolo que define los métodos y atributos para el routing de SABInvoices
protocol SABInvoicesRouterProtocol {
    // PRESENTER -> ROUTING
}
/// Protocolo que define los métodos y atributos para el Presenter de SABInvoices
protocol SABInvoicesPresenterProtocol {
    // VIEW -> PRESENTER
}
/// Protocolo que define los métodos y atributos para el Interactor de SABInvoices
protocol SABInvoicesInteractorInputProtocol {
    // PRESENTER -> INTERACTOR
}
/// Protocolo que define los métodos y atributos para el Interactor de SABInvoices
protocol SABInvoicesInteractorOutputProtocol {
    // INTERACTOR -> PRESENTER
}

