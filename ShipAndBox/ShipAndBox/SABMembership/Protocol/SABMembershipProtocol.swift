//
//  SABMembershipProtocol.swift
//  ShipAndBox
//
//  Created by IsitaFS003 on 13/09/21.
//  
//

import Foundation
import UIKit
/// Protocolo que define los métodos y atributos para el view de SABMembership
protocol SABMembershipViewProtocol {
    // PRESENTER -> VIEW
}
/// Protocolo que define los métodos y atributos para el routing de SABMembership
protocol SABMembershipRouterProtocol {
    // PRESENTER -> ROUTING
}
/// Protocolo que define los métodos y atributos para el Presenter de SABMembership
protocol SABMembershipPresenterProtocol {
    // VIEW -> PRESENTER
}
/// Protocolo que define los métodos y atributos para el Interactor de SABMembership
protocol SABMembershipInteractorInputProtocol {
    // PRESENTER -> INTERACTOR
}
/// Protocolo que define los métodos y atributos para el Interactor de SABMembership
protocol SABMembershipInteractorOutputProtocol {
    // INTERACTOR -> PRESENTER
}

