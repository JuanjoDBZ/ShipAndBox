//
//  CoOwnerCollectionViewCellProtocol.swift
//  ShipAndBox
//
//  Created by IsitaFS003 on 23/07/21.
//
import Foundation
import UIKit
/// Protocolo que define los métodos y atributos para el view de SABCoOwner
protocol CoOwnerCollectionViewCellViewProtocol {
    // PRESENTER -> VIEW
}
/// Protocolo que define los métodos y atributos para el routing de SABCoOwner
protocol CoOwnerCollectionViewCellRouterProtocol {
    // PRESENTER -> ROUTING
}
/// Protocolo que define los métodos y atributos para el Presenter de SABCoOwner
protocol CoOwnerCollectionViewCellPresenterProtocol {
    // VIEW -> PRESENTER
}
/// Protocolo que define los métodos y atributos para el Interactor de SABCoOwner
protocol CoOwnerCollectionViewCellInteractorInputProtocol {
    // PRESENTER -> INTERACTOR
}
/// Protocolo que define los métodos y atributos para el Interactor de SABCoOwner
protocol CoOwnerCollectionViewCellInteractorOutputProtocol {
    // INTERACTOR -> PRESENTER
}
