//
//  SABEditProfileProtocol.swift
//  ShipAndBox
//
//  Created by ISITA on 28/07/21.
//  
//

import Foundation
import UIKit
/// Protocolo que define los métodos y atributos para el view de SABEditProfile
protocol SABEditProfileViewProtocol {
    // PRESENTER -> VIEW
}
/// Protocolo que define los métodos y atributos para el routing de SABEditProfile
protocol SABEditProfileRouterProtocol {
    // PRESENTER -> ROUTING
    //func showNewView()
}
/// Protocolo que define los métodos y atributos para el Presenter de SABEditProfile
protocol SABEditProfilePresenterProtocol {
    // VIEW -> PRESENTER
}
/// Protocolo que define los métodos y atributos para el Interactor de SABEditProfile
protocol SABEditProfileInteractorInputProtocol {
    // PRESENTER -> INTERACTOR
}
/// Protocolo que define los métodos y atributos para el Interactor de SABEditProfile
protocol SABEditProfileInteractorOutputProtocol {
    // INTERACTOR -> PRESENTER
}

